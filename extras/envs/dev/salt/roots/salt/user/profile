if [ -n "$BASH_VERSION" ]; then
    if [ -f "$HOME/.bashrc" ]; then
        . "$HOME/.bashrc"
    fi
fi

export VARNISH_BANS_MANAGER_CONF="/vagrant/extras/envs/dev/varnish-bans-manager.conf"

export YUICOMPRESSOR_PATH="/usr/share/yui-compressor/yui-compressor.jar"

source $HOME/.virtualenvs/default/bin/activate
